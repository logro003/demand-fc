
from app import config

#config.MODEL_CONFIG.target


def main():

    # reading input data
    #config.MODEL_CONFIG.raw_data_path

    # verify dataframes

    # preprocess dataframe

    # generate features

    # training the model
    # model = training_xgb_model(df_with_features:pd.DataFrame, target_variable:str, features:list, max_depth:int, learning_rate:float, n_estimators:int)

    # making predicitons 

    ## Saving predictions to output folder
    #filename = 'next_day_predictions.csv'

